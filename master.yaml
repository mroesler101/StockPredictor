openapi: 3.0.0
info:
  version: 0.0.3-oas3
  title: StockPredictor
  description: Adjusted Closing Price Stock Ticker Predictor
  contact:
    name: Max Roesler
servers:
  - url: 'http://localhost:8080/'
paths:
  /timeframe/<start>/<end>/:
    post:
      tags:
        - TIMEFRAME
      summary: Start & end time of model
      operationId: nasdaq.timeframe
      description: Collects stock info within timeframe
      parameters:
           - in: query
             name: start
             required: true
             schema:
              type: string
              items:
               type: string
              minItems: 3
              maxItems: 3
           - in: query
             name: end
             required: true
             schema:
              type: string
              items:
               type: string
              minItems: 3
              maxItems: 3
      responses:
        '200':
          description: Successful dates & store dataframe of stocks
        '405':
          description: The inputs must be numbers
  /nasdaq/<ticker>/<days>/:
    get:
      tags:
        - TICKER
      operationId: nasdaq.nasdaq
      description: Uses ticker to predict stock price in number of days
      parameters:
           - in: path
             name: ticker
             required: true
             schema:
              type: string
           - in: path
             name: days
             required: true
             schema:
              type: string
      responses:
        '200':
          description: Graph of predicted price
        '405':
          description: The inputs need to be correct
